// СТАРТОВОЕ СОСТОЯНИЕ. Публичный API формально есть, фактически нет:
// `export *` показывает наружу всё, что окажется в файле, — включая то, что туда попадёт завтра.
// А то, что снаружи действительно нужно (badge), через него как раз не проходит.
export * from './model/order.ts';
